// postcss.config.js
/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    // Add autoprefixer for better compatibility
  },
};

export default config;